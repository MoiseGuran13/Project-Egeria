{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cd .. & git subtree push --prefix app/dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^4.0.4"
  },
  "dependencies": {
    "three": "^0.148.0"
  }
}
